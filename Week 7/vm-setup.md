# VM Setup
NOTE: this basic setup process should work for most vms, but the specific commands are for Ubuntu

1. connect to vm
2. check for updates
    - `sudo apt update`
3. update software that needs it
    - `sudo apt upgrade -y`
4. install software needed for your work
    - `sudo apt install openjdk-21-jdk -y`
5. download and run your software